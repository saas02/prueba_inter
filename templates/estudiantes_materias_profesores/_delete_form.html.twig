<form method="post" action="{{ path('estudiantes_materias_profesores_delete', {'id': estudiantes_materias_profesore.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estudiantes_materias_profesore.id) }}">
    <button class="btn">Delete</button>
</form>
